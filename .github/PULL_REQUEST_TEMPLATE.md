<!-- Keep this section's checkboxes and the COMPONENT_NAME/VERSION_NUMBER placeholders as they are. tk-cicd/.github/workflows/release-gating-pr.yml reads this exact wording; if you reword or remove it, update that workflow too (TK team). -->
## Release gating

Tell us whether this documentation change is tied to a product release.

* [ ] This change can publish immediately (no release dependency).
* [ ] This change must wait for a release. Component: `COMPONENT_NAME`, version: `VERSION_NUMBER`.
